# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
# (uv.lock IS committed)

# Environment / secrets
.env
.env.*
!.env.example

# Deploy: host-specific config + rendered runtime env (examples ARE committed)
/deploy/config.env
/deploy/config.*.env
!/deploy/config.*.env.example
/deploy/upstreamwx.env

# Release stamp written by deploy.sh / Pages CI (never committed; regenerated per deploy)
/frontend/version.json

# Runtime data cache (raw GRIB2/WBD downloads never committed)
/data/
/cache/
.hyriver_cache/
*.grib2
*.grib2.idx
!tests/fixtures/*.grib2

# Tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.mypy_cache/

# OS / editor
.DS_Store
*.swp
