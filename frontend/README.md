# frontend/ — reserved

The UpstreamWX PWA lands in **M0.4** (map location in → rendered SITREP out)
and is fleshed out in **M0.5** (full five-view IA, offline cache, PDF export). This
directory is intentionally empty until then; it is a separate (Node) toolchain kept
out of the Python backend project. See `roadmap.md` §M0.4–M0.5.
